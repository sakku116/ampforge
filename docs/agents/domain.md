# Domain Docs

Before exploring, read root `CONTEXT.md` and relevant `docs/adr/` decisions. If either is absent, proceed silently.

This is a single-context repository:

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use terminology defined in `CONTEXT.md`. Surface conflicts with an existing ADR explicitly rather than silently overriding it. Use `/domain-modeling` when a genuine vocabulary or decision gap needs recording.
